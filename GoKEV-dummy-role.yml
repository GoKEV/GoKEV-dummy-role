---
- name: Run this dummy role
  hosts: {{ input_hosts_var | default ('localhost') }}
  gather_facts: no

  vars:
    dummy_message: You can override this dummy_message variable with extra vars if you like.

  roles:
    - GoKEV.dummy-role

